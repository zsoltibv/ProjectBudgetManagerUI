import { DateToWordsPipePipe } from './date-to-words-pipe.pipe';

describe('DateToWordsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateToWordsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
